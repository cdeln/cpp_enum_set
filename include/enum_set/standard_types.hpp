#ifndef ENUM_SET_STANDARD_TYPES_HPP
#define ENUM_SET_STANDARD_TYPES_HPP

#include <cstddef>
#include <cstdint>

namespace enum_set
{

using std::nullptr_t;
using std::ptrdiff_t;
using std::size_t;
using std::uint8_t;

}  // namespace enum_set

#endif // ENUM_SET_STANDARD_TYPES_HPP
